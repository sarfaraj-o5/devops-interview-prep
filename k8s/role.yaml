## developer role(prod)
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: dev-read-only 
  namespace: prod
rules:
- apiGroups: [""]
  resources: ["pods","services", "configmaps"]
  verbs: ["get","list", "watch"]

  