egress:
- to:
  - namespaceSelector:
      matchLabels:
        kubernetes.io/metadata.name: kube-system
  ports: 
  - protocol: UDP 
    port: 53

## this single rule fixes 80% of NetworkPolicy broke my app incidents
