spec:
  rules:
  - host: app.company.com 
    http:
      paths: 
      - path: /
        backend:
          service:
            name: frontend
            port: 
              number: 80
      - path: /api 
        backend:
          service:
            name: backend
            port:
              number: 8080